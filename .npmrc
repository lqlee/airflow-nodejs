# Walmart internal npm registry — use this instead of public npmjs.org
# This registry proxies public packages with lower latency on Walmart's network.
registry=https://npm.ci.artifacts.walmart.com/artifactory/api/npm/npme-npm
